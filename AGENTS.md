# Repository Notes

This repository now contains a small Python MVP package in `src/lgm_compiler`
alongside the existing Rust workspace. Keep the Python package self-contained:
it should depend only on NumPy at runtime and pytest for tests.
